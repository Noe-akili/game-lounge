{
  "name": "game-lounge-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "esbuild server.ts --bundle --platform=node --format=esm --outfile=server.js --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --external:@neondatabase/serverless --external:sql.js",
    "start": "node server.js",
    "seed": "tsx seed.ts"
  },
  "dependencies": {
    "express": "^4.22.2",
    "jspdf": "^2.5.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^20.17.30",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.3",
    "typescript": "^5.6.3"
  }
}
